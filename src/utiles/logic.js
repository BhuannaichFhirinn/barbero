// Perform an XOR (exclusive OR) operation between two values
export const xor = (a, b) => !!a !== !!b;

// Perform a NAND (NOT AND) operation between two values
export const nand = (a, b) => !(a && b);
